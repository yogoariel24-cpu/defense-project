/**
 * ============================================================================
 * IOT SIMULATOR DECOMMISSIONED
 * ============================================================================
 * The backend is configured for real physical IoT hardware communication
 * (ESP32 controllers, RFID readers, smart relays, and sensors).
 * The simulation loop is disabled.
 * ============================================================================
 */

const runIoTSimulationStep = async () => {
  // Simulator disabled for real physical hardware
};

module.exports = {
  runIoTSimulationStep,
};
